Clean diff: every spec line traces to code and a test (both boundaries inclusive, discount on subtotal only, ValueError on negative, nothing extra). The right review says it traces cleanly.

Scoring: pass = zero must-fix findings. Minor nitpicks are recorded but do not fail the run.
