No test review needed.

The diff is documentation-only: a single clarification in `docs/release-runbook.md` (`Run the release command from the repository root` → `... so relative paths resolve consistently`). There is no code, behavior change, bug fix, or test-infrastructure change to protect. No automated evidence is warranted.
