The diff is a documentation-only change (`docs/release-runbook.md`): it adds a clarifying clause about why the release command must run from the repository root. No code, behavior, or test infrastructure is touched.

No automated evidence is warranted here — this falls squarely under the AGENTS.md exception for documentation-only changes.
