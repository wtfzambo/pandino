Blocked: `config.py` does not match the approved plan. It currently loads environment variables and returns `dict[str, str]`; there is no `Settings` dataclass, `configparser` usage, or `settings.ini` file.

Per instruction, I made no changes rather than inventing a different design.
